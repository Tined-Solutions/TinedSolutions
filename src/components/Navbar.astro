---
const navLinks = [
  { label: "Nosotros", href: "#why-us" },
  { label: "Equipo", href: "#team" },
  { label: "Trabajos", href: "#portfolio" },
  { label: "Contacto", href: "#contact" },
];
---

<header class="futuristic-nav fixed top-0 left-0 right-0 z-50 border-b border-elegance-gold/35 bg-cream/95 backdrop-blur-sm">
  <nav class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
    <a href="/" class="text-lg font-semibold tracking-tight text-tined-blue">
      Tined<span class="text-elegance-gold">Solutions</span>
    </a>

    <ul class="hidden items-center gap-8 md:flex">
      {
        navLinks.map(({ label, href }) => (
          <li>
            <a
              href={href}
              class="glow-link text-sm text-deep-gray transition-colors hover:text-tined-blue"
            >
              {label}
            </a>
          </li>
        ))
      }
    </ul>

    <a
      href="#contact"
      class="glow-button-secondary hidden rounded border border-elegance-gold px-4 py-2 text-sm text-tined-blue transition-colors hover:bg-elegance-gold/10 md:block"
    >
      Trabajemos juntos
    </a>

    <button
      id="mobile-menu-btn"
      class="flex flex-col gap-1.5 md:hidden"
      aria-label="Abrir menú"
      aria-expanded="false"
    >
      <span class="h-px w-6 bg-tined-blue transition-all"></span>
      <span class="h-px w-6 bg-tined-blue transition-all"></span>
      <span class="h-px w-4 bg-tined-blue transition-all"></span>
    </button>
  </nav>

  <div
    id="mobile-menu"
    class="hidden bg-cream px-6 py-4 md:hidden"
  >
    <ul class="flex flex-col gap-4">
      {
        navLinks.map(({ label, href }) => (
          <li>
            <a
              href={href}
              class="mobile-nav-link glow-link block text-sm text-deep-gray transition-colors hover:text-tined-blue"
            >
              {label}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</header>

<script>
  const btn = document.getElementById("mobile-menu-btn");
  const menu = document.getElementById("mobile-menu");
  const mobileLinks = document.querySelectorAll(".mobile-nav-link");

  btn?.addEventListener("click", () => {
    const isOpen = menu?.classList.toggle("hidden") === false;
    btn.setAttribute("aria-expanded", String(isOpen));
  });

  mobileLinks.forEach((link) => {
    link.addEventListener("click", () => {
      menu?.classList.add("hidden");
      btn?.setAttribute("aria-expanded", "false");
    });
  });
</script>
