---
import { reasons } from "../data/reasons";
---

<section id="why-us" class="reveal-on-scroll px-6 pt-6 pb-24 md:pt-10 md:pb-32">
  <div class="mx-auto max-w-6xl">
    <div class="mb-16 max-w-xl">
      <p class="mb-3 text-xs font-medium uppercase tracking-widest text-elegance-gold">
        ¿Por qué elegirnos?
      </p>
      <h2 class="font-tanker text-4xl leading-none text-tined-blue sm:text-5xl">
        Resultados que se notan <br />
        <span class="text-elegance-gold">desde el día uno.</span>
      </h2>
      <p class="mt-4 text-lg text-deep-gray">
        No hacemos solo webs, creamos herramientas para tu empresa.
      </p>
    </div>

    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
      {
        reasons.map(({ title, description, icon }) => (
          <article class="futuristic-card group reveal-on-scroll border border-elegance-gold/35 p-6 transition-colors hover:border-elegance-gold">
            <span class="mb-4 block text-xl text-elegance-gold transition-colors group-hover:text-tined-blue">
              {icon}
            </span>
            <h3 class="font-tanker mb-2 text-2xl tracking-wide text-tined-blue">{title}</h3>
            <p class="text-sm leading-relaxed text-deep-gray">{description}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
